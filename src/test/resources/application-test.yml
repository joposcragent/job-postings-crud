celery:
  orchestrator:
    base-url: http://localhost:9999

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: always
      schema-locations: classpath:job-postings-test-schema.sql
